/* === OGS Custom Profile Theme: ShadowPath Edition === */

/* 1. CURSEUR & TRAÎNÉE (Shadow Effect) */
html, body {
    /* Curseur dague/épée sombre personnalisé */
    cursor: url('https://cur.cursors-4u.net/games/gam-4/gam387.cur'), auto !important;
}

/* Effet de traînée sur les éléments interactifs */
a:hover, button:hover, .username:hover, .ratings-row:hover {
    cursor: url('https://cur.cursors-4u.net/games/gam-4/gam387.cur'), auto !important;
    transition: all 0.3s ease;
    /* Crée une aura violette qui semble suivre le mouvement sur l'élément */
    box-shadow: 0 0 20px 5px rgba(106, 13, 173, 0.4) !important;
}

/* 2. FOND GÉNÉRAL (Ambiance Eminence) */
body {
    background-color: #050505 !important;
    color: #e0e0e0 !important;
    font-family: 'Quicksand', "Segoe UI", sans-serif;
}

.container {
    max-width: none !important;
    /* Image de lune sombre style anime */
    background-image: url('https://fr.freepik.com/images-ia-gratuites/paysage-lune-dans-anime_234128294.htm#fromView=search&page=1&position=3&uuid=4942e3d9-a525-436b-bbaf-79082a2f0823&query=Fond+ecran+lune+sombre+anime') !important; 
    background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
    background-attachment: fixed;
}

/* 3. TYPOGRAPHIE (Noble & Dark) */
h1, h2, h3, .Player-username, .Player-rank {
    font-family: 'Cinzel', serif !important;
    color: #ffffff !important;
    text-shadow: 0 0 10px #6a0dad, 0 0 20px #9d4edd !important;
    letter-spacing: 2px;
}

/* 4. CONTENEURS (L'Ombre Translucide) */
.avatar-container, .ratings-row, .ratings-container, .col-sm-12, .col-sm-4 {
    background-color: rgba(10, 10, 10, 0.85) !important;
    border-radius: 12px;
    margin-bottom: 12px;
    border: 1px solid rgba(106, 13, 173, 0.5) !important;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.9);
    backdrop-filter: blur(8px); /* Effet de vitre fumée */
}

/* 5. PSEUDO & ANIMATION (Shadow Burst) */
.avatar-container .username {
  width: 350px;
  height: 100px;
  background: transparent;
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
}

.avatar-container .username span {
  transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

.avatar-container .username:hover span {
  transform: scale(1.15);
  color: #9d4edd !important;
  text-shadow: 0 0 25px #6a0dad;
}

/* 6. GRADIENT DE RANG (Violet Néon) */
.player-profile .rank {
    background: linear-gradient(135deg, #1a1a1a, #4b0082);
    color: #fff !important;
    border: 1px solid #9d4edd;
    padding: 4px 10px;
    box-shadow: 0 0 10px rgba(157, 78, 221, 0.5);
}

/* 7. AVATAR (Encadré de l'Éminence) */
.player-profile .avatar {
    border: 2px solid #6a0dad !important;
    box-shadow: 0 0 15px rgba(106, 13, 173, 0.7);
    filter: grayscale(20%) contrast(1.1);
}

/* 8. GRAPHES (Conversion des couleurs en Violet) */
.stats-container svg, .chart-container {
    filter: hue-rotate(270deg) brightness(0.9) saturate(1.5);
}

/* Nettoyage */
.ad-container, .banner-ad, .footer {
    display: none !important;
}

